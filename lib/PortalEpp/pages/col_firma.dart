import 'package:flutter/material.dart';

class ColFirma extends StatelessWidget {
  const ColFirma({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}