import 'package:flutter/material.dart';

class ColSolicitudes extends StatelessWidget {
  const ColSolicitudes({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}